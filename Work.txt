16:42 06.02.2011
Обдумать более удобный набор команд:
 - Раздельное управление ipv4 и ipv6 адресами (возможно через флаг ipv4 или ipv6 последним параметром)
 - Возможность изменять параметры конкретных алиасов (автоматический вынос из строки)
 - Как-то объединить логику добавления и обновления (чтобы избежать дублей хостов)

9:47 08.12.2010
Добавить поддержку IDN (в файле хранится в виде xn--, на экран выводится в декодированном виде).

0:04 08.12.2010
hosts add veg.dev www.veg.dev 127.0.0.1 # comment
Первый хост — основной, остальные — алиасы, порядок параметров произвольный, комменты после знака # и их можно писать без кавычек
hosts [command|cmd] запускает интерактивное редактирование (или просто ihosts?)
winhosts - редактор с GUI интерфейсом

13:03 02.12.2010
Решить проблемы:
 - Программа не позволяет одному хосту привязать сразу ipv4 и ipv6 одновременно
 - В одной строке нельзя разместить несколько хостов
 - Нужна более грамотная обработка ошибок (чтобы возможность записи в файл проверялась сразу)
Подумать:
 - hosts recreate - создаёт новый файл hosts с одной записью (localhost)
 - Какой-то символ, который позволял бы объединять несколько операций (по аналогии с && в командной строке, но внутри команды hosts)


17:49 01.12.2010
C# 3.5 (встроен в Windows 7)
hosts open (открывать в блокноте, который ассоциирован с .txt)
hosts backup
hosts restore
hosts append (дописывает содержимое другого hosts)
Во view предотвратить вставку лишних *
Если по требованию изменить свойство и оно на самом деле установлено в то же, что и было, выводить об этом инфу (например вместо SHOWN писать ALREADY)

16:41 30.11.2010
Я это сделал =) Хотя по работе уже и не нужно...

21:36 22.07.2009
Задачи консольной версии:
добавление новых хостов
удаление хостов
отключение отдельных позиций
группы хостов
для каждой группы свои настройки по умолчанию
профили (или их заменят группы?)

hosts rem localhost
hosts set localhost 127.0.0.1
hosts off localhost
hosts disable localhost
hosts enable localhost
hosts add localhost 127.0.0.1
hosts clean
hosts list
hosts profiles
hosts setprofile work

11:34 28.01.2009
Консольная утилита для редактирования hosts.
Отобразить список хостов
host
Удалить localhost с поддоменами
host /d *localhost
Добавить хост
host localhost 127.0.0.1